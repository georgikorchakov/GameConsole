# Fibonachi
int nthNumber = 6;
int currentNumber = 1;

int previouspreviousNumber = 0;
int previousNumber = 0;

for (int i = 1; i < nthNumber; i = i + 1) {
    previouspreviousNumber = previousNumber;
    previousNumber = currentNumber;
    currentNumber = previouspreviousNumber + previousNumber;
}
